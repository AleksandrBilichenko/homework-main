const logBigNums = () => {
    var array = [ 254, 115, 78,25, 91, 45, 37];
    var maximaizer = 50;
    for (var i = 0; i < array.length; i++)
        if (array[i] >= maximaizer) 
        alert(array[i])
}

logBigNums();